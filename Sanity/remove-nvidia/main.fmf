summary: Test NVIDIA driver removal
description: |
  Verify that removing nvidia without a version removes the installed driver
  stack and kernel modules while preserving unrelated packages. Install mock
  RPMs with DNF so the test exercises removal independently of radii's
  installation behavior, without requiring NVIDIA hardware.
require:
  - dnf-plugins-core
  - type: library
    nick: radii
    name: /Library/mock-rpms

/single:
  order: 60
  summary+: ' (single kmod)'

/multiple-kmods:
  order: 61
  summary+: ' (multiple kmods)'
  description+: |
    Leftover kernel modules from an older driver version must also be removed.
  environment+:
    RADII_EXTRA_KMOD_VERSION: 580.178.04
